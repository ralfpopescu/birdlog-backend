type JournalEntry {
  month: Int!
  day: Int!
  year: Int!
  entry: String
}

input JournalDate {
  month: Int!
  day: Int!
  year: Int!
}

extend type Query {
  journalEntry(date: JournalDate): JournalEntry
}
